platform:
  database:
    tenantDatabasePrefix: platform_extend
  swagger:
    docket:
      msg:
        title: 消息服务
        base-package: com.qhh.platform.msg.controller;com.qhh.platform.sms.controller
  cache:
    def:
      keyPrefix: ${spring.profiles.active}
    configs:
      platform:
        timeToLive: 1h
        keyPrefix: aaa
      test:
        timeToLive: 2h

server:
  port: 8768
